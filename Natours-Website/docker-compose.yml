version: '3.9'

services:
 #Mongodb services
 mongo_db:
  container_name: db_container
  image: mongo:lates
  restart: always
  volumes:
   - mongo_db:/data/db
 
 #Node API service
 api:
  build: .
  ports:
   - 3000:3000
  environment: